# Adaptive Multifactor Graphical Authentication (Flask)

This project implements an adaptive multifactor authentication system using:
- A graphical password (knowledge factor)
- Context-aware risk analysis (contextual factor)
- Time-based one-time password (possession factor)

---

## Setup

```bash
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate
pip install -r requirements.txt

